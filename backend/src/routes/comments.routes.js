// Comments are handled via /tickets/:ticketId/comments in tickets.routes.js.
// This file is a placeholder for any future standalone comment endpoints.
const router = require('express').Router();
module.exports = router;
