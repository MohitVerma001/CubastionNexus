// Attachments are handled via /tickets/:ticketId/attachments in tickets.routes.js.
// This file is a placeholder for any future standalone attachment endpoints.
const router = require('express').Router();
module.exports = router;
